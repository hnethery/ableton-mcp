# Bolt's Journal

## 2024-05-22 - [Buffer Management]
**Learning:** String concatenation in a loop (buffer += chunk) is O(N^2) in Python. Using a list of strings and joining them is O(N).
**Action:** Inspect buffer handling code in network listeners.
